# Core data science
numpy==2.3.5
pandas==2.3.0
scipy==1.16.3
scikit-learn==1.7.0

# Embeddings (CPU-friendly sentence encoder)
sentence-transformers==5.6.0
transformers==5.12.1
torch>=2.0.0

# Sparse retrieval
rank-bm25==0.2.2

# Learning-to-rank
lightgbm>=4.0.0

# Config and serialization
pyyaml>=6.0
pyarrow>=14.0.0   # for parquet

# Sandbox (Streamlit — not needed in rank step)
streamlit>=1.30.0

# LLM semantic scoring (precompute phase only — not needed during ranking)
anthropic>=0.40.0

# Deck generation (optional — Marp CLI or pandoc)
# Not a Python dep; install separately: npm install -g @marp-team/marp-cli
